include:
  - local: build_multiarch.yml
  - local: trivy-scan.yml
  - local: deploy_argo.yml
  - local: purge_cache.yml
  # - local: kube-score.yml
  
variables:
  K8S_NAMESPACE: backend
  ARGOCD_APP_PROJECT: ${K8S_NAMESPACE}
  ARGOCD_APP_DEST_CLUSTER_NAME: prod
