include:
  - local: functions/build_kaniko.yml

Build:ARM64:
  extends: .build
  variables:
    ARCH: "arm64"
    RUNNER_TAG: "micro-arm64"
    KUBERNETES_NODE_SELECTOR_ARCH: "kubernetes.io/arch=${ARCH}"
