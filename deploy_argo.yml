include:
  - local: functions/install_argocd_app.yml

Deploy:
  stage: deploy
  extends: .deploy
  environment:
    name: stage
    url: https://from-the-lamp.com
  rules:
  - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
