include:
  - local: functions/argocd_app_update.yml

Stage:Deploy:
  stage: deploy
  extends: .deploy
  environment:
    name: stage
    url: https://from-the-lamp.work
  needs:
    - Build
  rules:
  - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
