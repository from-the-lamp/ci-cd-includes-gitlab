include:
  - local: functions/argocd_create_core.yml

Create:Appset:
  stage: deploy
  extends: .create
  variables:
    RUNNER_TAG: micro-arm64
    APP_TYPE: appset
    APP_FILE_PATH: applicationset.yml
