include:
  - local: functions/build_and_push_docker.yml

stages:
  - Build
  
Build:
  extends: .build
  tags:
    - k8s-arm64
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
      when: on_success
    - when: never